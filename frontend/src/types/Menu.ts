export type MenuType = {
    id: string
    name: string
    desc: string
    price: number
    image: string
    quantity: number
    available: number
}

export type MenuTypeInCart = {
    id: string
    name: string
    price: number
    image: string
}